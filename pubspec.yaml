name: barberia_app
description: App móvil Flutter para consumir la API Barbería
publish_to: 'none'
version: 0.0.1
environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  dio: ^5.1.2
  flutter_secure_storage: ^8.0.0
  intl: ^0.18.1
  flutter_datetime_picker: ^1.5.1
  connectivity_plus: ^5.0.0
  shared_preferences: ^2.1.0

flutter:
  uses-material-design: true
